---
interface Props {
  title: string;
  sub?: string;
}
const { title, sub } = Astro.props;
---

<div class="max-w-2xl">
  <h2 class="font-display text-2xl font-semibold tracking-tight text-snow sm:text-3xl">{title}</h2>
  {sub && <p class="mt-2.5 leading-relaxed text-mist">{sub}</p>}
</div>
